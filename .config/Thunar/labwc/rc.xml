<!--
  This is a very simple config file with many options missing. For a complete
  set of options with comments, see docs/rc.xml.all
-->

<labwc_config>

  <theme>
    <name>Clearlooks-3.4</name>
    <cornerRadius>8</cornerRadius>
    <font name="sans" size="10" />
  </theme>

  <keyboard>
    <default />
    <!-- Use a different terminal emulator -->
    <keybind key="W-Return">
      <action name="Execute" command="foot" />
    </keybind>
    <!--
      Remove a previously defined keybind
      A shorter alternative is <keybind key="W-F4" />
    -->
    <keybind key="W-F4">
      <action name="None" />
    </keybind>
    
    <keybind key="W-v">
  <action name="Execute">
    <command>/home/peter/.local/bin/clip-menu.sh</command>
  </action>
  </keybind>

  <!--
  <keybind key="Print">
    <action name="Execute">
    <command>sh -c 'grim ~/screenshots/screenshot-$(date +%Y%m%d-%H%M%S).png'</command>
  </action>
  </keybind>
  -->

  <keybind key="Print">
    <action name="Execute">
      <command>/home/peter/.local/bin/screenshot.sh</command>
    </action>
  </keybind>


  <keybind key="S-Print">
    <action name="Execute">
      <command>sh -c 'grim -g "$(slurp)" ~/screenshots/screenshot-$(date +%Y%m%d-%H%M%S).png'</command>
    </action>
</keybind>



  </keyboard>

  <mouse>
    <default />
    <!-- Show a custom menu on desktop right click -->
    <context name="Root">
      <mousebind button="Right" action="Press">
        <action name="ShowMenu" menu="some-custom-menu" />
      </mousebind>
    </context>
  </mouse>

</labwc_config>
